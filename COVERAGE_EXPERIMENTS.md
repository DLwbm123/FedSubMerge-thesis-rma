# Remaining setting coverage on GPU 3

Authorized scope: PathMNIST alpha=0.1 (four continual sequences and three independent references), Hyper-Kvasir alpha=0.1 (four sequences and nine references), and the existing six-class skin cache (four sequences and two references). Total: 26 jobs, seed 2025 only. Methods are FedAvg, Fed-GPM, FedSubMerge and FedSubMerge-AD. OrganAMNIST is explicitly excluded by the user. Existing completed runs are not repeated.

Reuse the existing alpha=0.1 training-only client partitions and official/global validation and test data. Task sizes remain `[2,2,2,3]` for PathMNIST and `[2]*10` for Hyper-Kvasir. Matching fresh-task references start from task 2 and share the same protocol. Training remains 20 rounds per task, 3 local epochs, batch 64, original full-output cross entropy and known-task masked evaluation. Only CUDA allocator limits and independent-job concurrency change; optimizer batch sizes and model precision do not change.

The user explicitly accepted the existing skin cache with 24,827 images, rather than the 24,180-image total stated in the historical thesis table. It has six classes and tasks `[2,2,2]`. Reuse the four-source train and local-holdout index maps: 15,966 training, 3,991 validation, 4,870 separate test images. Every cached training-pool image belongs to exactly one train or validation subset; the subsets are disjoint. No images are discarded or newly sampled to force the historical count. Inputs are the 128 cache resized online to 256 with the shared augmentation protocol. Validation is the union of existing client holdouts; the global test file is not used for model selection. AD uses two related clients. Data version and split differences remain explicit; these runs do not reproduce the historical ACC values.

Resource accounting follows RESOURCE_EXPERIMENTS.md. MPE measures classifier expansion, while auxiliary states, CUDA peaks, subspace construction/merge and training time are recorded separately. DRR includes samples contributing to retained representations. Concurrent GPU timings describe this execution and are not isolated speed rankings.

The GPU3-only finite queue admits at most three 128-input jobs simultaneously, with a 12,500 MiB admission budget and 28% allocator cap per job. A 256-input job has a 37,000 MiB admission budget and a 90% allocator cap and runs without other jobs from this queue. Total admitted budgets never exceed 38,000 MiB, and admission also checks fresh free memory. Finished slots are refilled automatically. Failed jobs retain failure records while other independent jobs continue; no automatic compute retries or extra experiments are added. No existing process is terminated.

Parent and child OS command lines use `python -u entry.py`; dataset/method/storage arguments are supplied internally from a private JSON configuration and environment variables. All data, logs, checkpoints, cache and temporary files remain on the verified remote-home NFS filesystem. Images are memory-mapped in place. Allow 40 GiB for outputs, logs and temporary checkpoint copies; storage preparation found over 350 TiB free.

Remote root: `/remote-home/wangbomin/FedSubMerge_thesis/runs/coverage_20260913`. Private configuration: `jobs.json`; state: `queue.json`; logs: `logs/00.log` through `logs/25.log`. Dedicated tmux server/session: `q0913`. Formal runs use SwanLab. Startup checks establish launch only; final matrices and paired reference checks are required before filling table cells. No ongoing chat monitoring is scheduled.

Completion verified on 2026-09-15: all 26 jobs succeeded. See COVERAGE_COMPLETION_REPORT.md and results/coverage_resources_summary.csv.
