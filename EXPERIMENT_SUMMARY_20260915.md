# 联邦持续学习实验整理与必要工作（2026-09-15）

当前已完成 60 次正式运行：30 条完整持续学习序列、30 个独立任务参考。最新一批 26 项全部退出码为 0，已核对完整记录、模型文件存在性及 RMA 配对。实验结果未按论文历史精度改写，也未因结果不理想而重跑。

| 设置 | 完整序列 | 独立参考 | 已覆盖方法 |
|---|---:|---:|---|
| PathMNIST α=0.3 | 7 | 3 | 四核心方法及 FOT、Fed-DER、TARGET |
| PathMNIST α=0.1 | 4 | 3 | 四核心方法 |
| Hyper-Kvasir α=0.3 | 7 | 9 | 四核心方法及 FOT、Fed-DER、TARGET |
| Hyper-Kvasir α=0.1 | 4 | 9 | 四核心方法 |
| Hyper-Kvasir quantity-skew | 4 | 4 | 四核心方法 |
| 皮肤病变 feature-skew | 4 | 2 | 四核心方法 |

四核心方法是 FedAvg、Fed-GPM、FedSubMerge、FedSubMerge-AD。全部 seed=2025。OrganAMNIST 按用户要求不做，因此七个原设置中的六个完成核心方法的指标覆盖；这不等于原主表全部十四个方法都具有新指标。

## 实际结论

- 所有已测模型 MPE=0：分类网络固定，不发生参数扩张。该指标不计辅助状态，不能据此声称零额外存储。
- 六个设置中，FedSubMerge 系列的客户端辅助状态逻辑总量均小于 Fed-GPM；具体数值见下表。该比较包含表示类型、数值精度与实现差异，GPM 为 float64 基，PGS 为 float32 基，不能解释为纯算法压缩比或峰值主存比较。
- FedSubMerge 系列的 DRR 高于 Fed-GPM：PGS 使用较多当前源样本构建表示。这里 DRR 不是历史原图存储率；不能写成数据依赖全面更低。
- ACC、BWTR 的优势并不覆盖所有设置。例如 PathMNIST α=0.1 的 Fed-GPM 实测 ACC 为 69.56%，FedSubMerge/AD 为 54.70%/60.13%；Hyper-Kvasir α=0.1 的 FedAvg 实测 ACC 为 73.66%，FedSubMerge/AD 为 69.54%/68.29%。不要用不同批次的历史 ACC 与当前 BWTR 联合证明性能优势。
- RMA 反映相对于独立学习参考的当前任务学习能力，不是最终平均准确率；RMA 接近或超过 1 可以同时伴随明显遗忘。

| 设置 | Fed-GPM 辅助 MiB | FedSubMerge 辅助 MiB | AD 辅助 MiB |
|---|---:|---:|---:|
| pathmnist α=0.3 | 817.82 | 3.49 | 3.13 |
| pathmnist α=0.1 | 563.79 | 2.55 | 1.78 |
| hyperkvasir α=0.3 | 445.21 | 5.54 | 5.49 |
| hyperkvasir α=0.1 | 300.54 | 1.63 | 1.90 |
| hyperkvasir quantity | 248.91 | 3.10 | 2.99 |
| skin feature | 56.70 | 6.11 | 5.47 |

## 现在仍必要的工作

**就“以四核心方法将平台指标覆盖三类偏斜设置”的目标，目前没有必须追加的训练实验。建议停止扩展实验，优先完成以下结果与写作工作。**

1. 统一论文中的指标定义、比值/百分数单位和结论。原 BWT 不能仅改名称成为 BWTR；原逐任务矩阵如果能恢复，可重新计算历史 BWTR。本次指标由新矩阵得到，不能声称对应历史 ACC。
2. 在论文实验设置或正文交代数据与实现来源。FedCL.tex 按用户要求保留干净表格和历史 ACC；独立实测结果表提供相同运行内的 ACC/RMA/BWTR/MPE/DRR。正式分析必须保持来源清晰；若要求完全同一次实验的主表，需使用同一实测矩阵的 ACC 或恢复原始实验记录。
3. 皮肤病变注明用户接受的 24,827 张缓存版本和 15,966/3,991/4,870 划分，与旧表 24,180 张区分。不能将现有缓存改写成原论文版本。
4. 将辅助状态存储写入资源分析，否则 MPE 全为零无法呈现各方法开销差异。计时受共享 GPU 与实现影响，不用于严格速度排名。
5. TARGET 的真实历史图像使用与生成图像代理分开报告；公共 DRR 列保留空白。增加训练不能解决计量口径问题。

只有改变论文目标时才新增实验：若要求其余方法也填满全部设置的指标，需要适配与训练那些缺失方法；若重新纳入 OrganAMNIST，先解决原划分不匹配。多 seed、新算法变体、更多阈值扫描、为追求论文精度而反复重跑均不属于本轮必要工作。

## 交付文件

- results/all_measured_metrics.csv / .md：30 行同运行实测指标与资源数据。
- results/coverage_resources_complete.json：最新 26 项的已校验记录与独立参考。
- results/target_auxiliary_components.csv：TARGET 辅助开销分项。
- thesis_tables/FedCL.tex：三张论文表，历史 ACC 保留，已完成的新指标已填入，其余留空；该本地排版稿不代替公开实测结果表。

未执行额外训练。图像、私有索引、权重、完整日志保留在服务器，不公开发布。
